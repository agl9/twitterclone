<%= simple_form_for(@tweet) do |form| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% @tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class ='control'>
      <%= form.input :tweet, label:"Tweet about:",input_html: {class:'textarea'}, wrapper: false, label_html:{class:'label'},placeholder: 'What is on your mind?'%>
    </div>
  </div>

  <%= form.button :submit, class: 'button is-info' %>

<% end %>
